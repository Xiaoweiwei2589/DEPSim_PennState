# Figures to add (placeholders)

1. architecture.png
   - PSUDEPSim ↔ CHARM loop, similar to the standard schematic.

2. directory_tree.png
   - high-level DEPSim directory structure (aircraft models separated from core simulator).

3. workflow.png
   - Trim → Linearize → Control Design → Simulate → Post-process.

4. example_vehicle.png
   - OpenVSP render (only if you have rights to share it).